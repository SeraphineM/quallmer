% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_icc.R
\name{reliability_icc}
\alias{reliability_icc}
\title{Intraclass correlation coefficient}
\usage{
reliability_icc(
  ratings,
  model = c("oneway", "twoway"),
  type = c("consistency", "agreement"),
  unit = c("single", "average"),
  r0 = 0,
  conf.level = 0.95
)
}
\arguments{
\item{ratings}{A \verb{subjects x raters} matrix or data.frame of numeric
ratings. Rows are objects of measurement (subjects); columns are
raters. Must not contain \code{NA}.}

\item{model}{\code{"oneway"} (each subject rated by a different random set
of raters) or \code{"twoway"} (the same \code{k} raters rate every subject).}

\item{type}{\code{"consistency"} (column variance excluded -- relative
agreement) or \code{"agreement"} (column variance included -- absolute
agreement). Ignored for \code{model = "oneway"}.}

\item{unit}{\code{"single"} (reliability of one rater's score) or
\code{"average"} (reliability of the mean across \code{k} raters; the
Spearman-Brown stepped-up form).}

\item{r0}{Null-hypothesis value for the F-test. Default 0 tests
\code{H0: ICC = 0}.}

\item{conf.level}{Confidence level for the CI on the population ICC
(default 0.95).}
}
\value{
A list with elements:
\describe{
\item{\code{method}}{Short label, e.g. \code{"icc_2_1"} or \code{"icc_3_k"}.}
\item{\code{value}}{The ICC estimate.}
\item{\code{ci_lower}, \code{ci_upper}}{Confidence interval bounds at
\code{conf.level}.}
\item{\code{per_value}}{\code{NULL} (ICC has no per-category breakdown).}
\item{\code{n_observers}, \code{n_units}, \code{n_pairable}}{Counts (k, n, k*n).}
\item{\code{model}, \code{type}, \code{unit}}{The configuration that produced the
ICC.}
\item{\code{icc_name}}{Canonical Shrout-Fleiss name, e.g. \code{"ICC(2,1)"}.}
\item{\code{F_value}, \code{df1}, \code{df2}, \code{p_value}, \code{r0}}{F-test of
\code{H0: ICC = r0}.}
}
}
\description{
Native implementation of the intraclass correlation coefficient (ICC)
family for a \verb{subjects x raters} matrix of interval/ratio ratings.
Six forms are exposed via \code{model}/\code{type}/\code{unit}, following the
Shrout-Fleiss naming and the McGraw-Wong calculation tables:
}
\details{
\tabular{lllll}{
   \code{model} \tab \code{type} \tab \code{unit} \tab Shrout & Fleiss \tab McGraw & Wong \cr
   oneway \tab (n/a) \tab single \tab ICC(1,1) \tab ICC(1) \cr
   oneway \tab (n/a) \tab average \tab ICC(1,k) \tab ICC(k) \cr
   twoway \tab consistency \tab single \tab ICC(3,1) \tab ICC(C,1) \cr
   twoway \tab consistency \tab average \tab ICC(3,k) \tab ICC(C,k) \cr
   twoway \tab agreement \tab single \tab ICC(2,1) \tab ICC(A,1) \cr
   twoway \tab agreement \tab average \tab ICC(2,k) \tab ICC(A,k) \cr
}


For \code{model = "oneway"} the \code{type} argument is ignored (only one form
exists). The two-way random and two-way mixed models share the same
calculations; they differ only in interpretation (whether the column
factor levels are treated as a random sample or as fixed). See Koo &
Li (2016) for guidance on selecting a form.
}
\references{
Shrout, P. E., & Fleiss, J. L. (1979). Intraclass correlations: Uses
in assessing rater reliability. \emph{Psychological Bulletin}, 86(2),
420-428. \doi{10.1037/0033-2909.86.2.420}

McGraw, K. O., & Wong, S. P. (1996). Forming inferences about some
intraclass correlation coefficients. \emph{Psychological Methods}, 1(1),
30-46. \doi{10.1037/1082-989X.1.1.30}

Koo, T. K., & Li, M. Y. (2016). A guideline of selecting and reporting
intraclass correlation coefficients for reliability research.
\emph{Journal of Chiropractic Medicine}, 15(2), 155-163.
\doi{10.1016/j.jcm.2016.02.012}
}
\keyword{internal}
