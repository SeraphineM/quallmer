% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qlm_backfill.R
\name{qlm_backfill}
\alias{qlm_backfill}
\title{Re-code the units a run failed on}
\usage{
qlm_backfill(x, ..., model = NULL, attempts = 2L)
}
\arguments{
\item{x}{qlm_coded; a coded object produced by \code{\link[=qlm_code]{qlm_code()}} or \code{\link[=qlm_replicate]{qlm_replicate()}}.}

\item{...}{optional overrides passed to \code{\link[=qlm_code]{qlm_code()}} for the backfill
passes, such as \code{params}, \code{max_active} or \code{on_error}. Any setting not
overridden is restored from the original run, as \code{\link[=qlm_replicate]{qlm_replicate()}} does,
with the same rule for credentials and endpoint settings when the
provider changes. The codebook cannot be changed.}

\item{model}{character or \code{NULL}; the model for the passes, in the form
used by \code{\link[=qlm_code]{qlm_code()}}. \code{NULL} (default) uses the run's own model.}

\item{attempts}{integer; the maximum number of passes. Default is 2. A pass that
recovers nothing ends the backfill early, since the failures that remain
are then evidently not transient.}
}
\value{
\code{x}, with the recovered units filled in and \code{backfill} added to
its object metadata. The run name, parent, codebook and inputs are
unchanged.
}
\description{
A coding run over a real corpus rarely comes back complete. Requests time
out or are rate-limited, a provider refuses a text on one pass and codes it
on the next, an endpoint accepts a schema and ignores it for a few units.
The failed units sit in the \code{qlm_coded} object as \code{NA} rows, listed by
\code{\link[=qlm_failures]{qlm_failures()}}. \code{qlm_backfill()} re-codes only those units and merges
what comes back into the original object. Everything that succeeded the
first time is left exactly as it was.
}
\details{
By default the passes use the run's own model, codebook and settings, so
the result is what the run should have produced. A different \code{model} can
be given, for units the original model consistently refuses or cannot fit
in its context window; the object then records which units were coded by
which model, and \code{print()} and \code{\link[=qlm_trail]{qlm_trail()}} say so, since a result coded
by two instruments has to be disclosed as one.

Which units are re-coded is decided afresh on every pass, from the object as
it then stands, by the same test \code{\link[=qlm_failures]{qlm_failures()}} uses: a unit that carries
an \code{.error}, or whose required scalar properties are all \code{NA}. Two kinds of
failure are left alone, because re-sending the same request cannot change
the outcome:
\itemize{
\item a text the provider rejected as longer than the model's context window,
unless \code{model} changes;
\item a response cut off at the \code{max_tokens} limit, unless \code{model} changes or
the backfill raises the limit, by passing a \code{params(max_tokens = )}
higher than the run's own. Other \code{params} leave the limit where it was,
and so leave those units alone.
}

Content refusals are deliberately retried. They look deterministic and are
not: the same document is refused on one pass and coded on the next, at
more than one provider.

Each pass is an ordinary \code{\link[=qlm_code]{qlm_code()}} call over the failed units, on the
path the original run took (a run that fell back to JSON mode is backfilled
in JSON mode; with a different endpoint the path is chosen afresh), and
always as a parallel call: a run coded through the batch API is backfilled
through the parallel API, with the same model and settings, and any
batch-only arguments (\code{path}, \code{wait}, \code{ignore_hash}) set aside. A pass that
fails outright on the first attempt is an error, since nothing has been
gained yet and the cause is most likely configuration; on a later pass it
is a warning, and what earlier passes recovered is kept.

Units are identified by \code{.id} throughout: the failed units' inputs are
looked up by \code{.id}, so an object whose rows have been reordered or subset
is backfilled correctly, and the merge is by \code{.id}. Rows keep their order;
a unit is replaced only when the retry produced a usable coding, so a retry
that failed again never overwrites anything, though its \code{.error} is
recorded as the latest reason. Token and cost columns, when present, are
summed across all attempts, since a failed request may still have been
billed. The passes are recorded in the object metadata as \code{backfill}, one
entry per pass with its timestamp, the model if it differed from the run's,
the overrides, and the \code{.id}s attempted and recovered, so the result can say
which of its rows came from which pass and which model. \code{\link[=qlm_replicate]{qlm_replicate()}}
replays these passes on a replication, so that a replication of a completed
run is completed on the same terms.
}
\examples{
\dontrun{
coded <- qlm_code(texts, codebook, model = "openai/gpt-4o-mini",
                  on_error = "continue")
qlm_failures(coded)

filled <- qlm_backfill(coded)
qlm_failures(filled)
qlm_meta(filled, "backfill", type = "object")

# Responses cut off at the output limit are retried only with a higher one
filled <- qlm_backfill(coded, params = ellmer::params(max_tokens = 32000))

# Units one model refuses or cannot fit, coded by another; the result
# records which units came from which model
filled <- qlm_backfill(filled, model = "deepseek/deepseek-chat")
}

}
\seealso{
\code{\link[=qlm_failures]{qlm_failures()}} for the units a run failed on and why;
\code{\link[=qlm_code]{qlm_code()}}, whose \code{backfill_attempts} completes a run in the same call;
\code{\link[=qlm_replicate]{qlm_replicate()}} to re-run a whole coding.
}
