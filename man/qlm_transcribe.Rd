% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qlm_transcribe.R
\name{qlm_transcribe}
\alias{qlm_transcribe}
\title{Transcribe audio recordings}
\usage{
qlm_transcribe(
  x,
  model = "openai/gpt-4o-mini-transcribe",
  language = NULL,
  prompt = NULL,
  api_key = NULL,
  base_url = NULL,
  max_active = 10,
  rpm = 60,
  on_error = c("continue", "return", "stop"),
  ...
)
}
\arguments{
\item{x}{character; paths of audio files, or \code{http(s)} URLs of them,
optionally named. See the section "Names and identifiers".}

\item{model}{character; the transcription model in \code{"provider/model"}
form. See the section "Backends".}

\item{language}{character; the ISO 639-1 code of the language spoken,
such as \code{"en"} or \code{"zh"}, or \code{NULL} to let the model detect it. Sent to
the OpenAI endpoint as its \code{language} field; added to the instruction
for a Gemini model.}

\item{prompt}{character; optional text to guide the transcription, such
as names and terms the recording contains or the style of punctuation
wanted. Sent to the OpenAI endpoint as its \code{prompt} field; added to the
instruction for a Gemini model.}

\item{api_key}{character; the API key. \code{NULL} reads the environment
variable the provider uses, \code{OPENAI_API_KEY} or \code{GEMINI_API_KEY}. The
key is never recorded.}

\item{base_url}{character; the endpoint to send the requests to. For the
OpenAI backend, the prefix before \verb{/audio/transcriptions}; \code{NULL} is
\verb{https://api.openai.com/v1}. For a Gemini model, passed to ellmer's
chat constructor. Recorded with any credential it carries redacted.}

\item{max_active}{integer; the number of requests in flight at once, as
in \code{\link[ellmer:parallel_chat]{ellmer::parallel_chat()}}.}

\item{rpm}{integer; the request rate in requests per minute. The default
is below ellmer's because transcription endpoints have their own,
lower, rate limits. A rate-limited request is retried after the delay
the provider asks for.}

\item{on_error}{character; what to do when a transcription fails. See the
section "Failures".}

\item{...}{Reserved; must be empty.}
}
\value{
A named character vector of class \code{qlm_transcript}, one element
per element of \code{x} in the same order, with attribute \code{provenance}, a
data frame with one row per element:
\describe{
\item{\code{.id}}{the element's name.}
\item{\code{status}}{\code{"ok"}, \code{"failed"} or \code{"unsubmitted"}.}
\item{\code{file}}{the basename of the file, or of the URL's path.}
\item{\code{source_url}}{the URL, redacted, or \code{NA} for a local file.}
\item{\code{.error}}{the failure message, or \code{NA}.}
\item{\code{size}, \code{sha256}}{the bytes transcribed and their hash; \code{NA}
when a download failed.}
\item{\code{model}, \code{registered}}{the model, and the \code{"provider/model"}
pair when it was accepted by a session registration.}
\item{\code{language}, \code{prompt}, \code{base_url}}{as given, or \code{NA}.}
\item{\code{timestamp}}{when the response arrived, or \code{NA}.}
\item{\code{ellmer_version}}{for the Gemini backend, or \code{NA}.}
\item{\code{usage}}{a list column holding what the provider reported.}
}
Subsetting with \code{[}, renaming with \verb{names<-} and concatenating with
\code{c()} keep the table aligned with the elements; \code{as.character()} drops
it.
}
\description{
Transcribes audio files with a speech-to-text model and returns the
transcripts as a character vector that carries the provenance of each
one: the file it came from and its hash, the model, the language and
prompt given, the time, and the usage the provider reported. Passed to
\code{\link[=qlm_code]{qlm_code()}} with a text codebook, the transcripts are coded as ordinary
text and that provenance is recorded with the run, so \code{\link[=qlm_trail]{qlm_trail()}}
documents the transcription as part of the measurement instrument and the
same text can be coded again by \code{\link[=qlm_replicate]{qlm_replicate()}} or \code{\link[=qlm_backfill]{qlm_backfill()}}
without another transcription request.
}
\details{
This is the two-stage route to audio: transcribe once, then code the text
with any provider. The single-pass route, a codebook with
\code{input_type = "audio"}, sends the recording itself to a model that can
hear it; see the "Audio input" section of \code{\link[=qlm_code]{qlm_code()}} for the providers
that accept it.
}
\section{Backends}{


The backend is chosen from the provider prefix of \code{model}:
\itemize{
\item \verb{openai/}: the transcription endpoint, \verb{/v1/audio/transcriptions},
which ellmer does not wrap. Models \verb{gpt-4o-mini-transcribe} (the
default), \verb{gpt-4o-transcribe} and \code{whisper-1}. Each file must be at
most 25 MB and one of \code{flac}, \code{mp3}, \code{mp4}, \code{mpeg}, \code{mpga}, \code{m4a},
\code{ogg}, \code{wav} or \code{webm}. The endpoint reports usage as audio and text
tokens for the \verb{gpt-4o} models and as seconds of audio for \code{whisper-1}.
\code{base_url} reaches any host that serves the same request shape.
\item \verb{google_gemini/}: a Gemini chat model asked to transcribe verbatim,
reached through ellmer with the recording uploaded first. Any model
\code{\link[=qlm_code]{qlm_code()}} accepts for audio input works; the same capability check
applies, and a model outside the recognised families can be accepted
for the session with \code{\link[=qlm_register_model]{qlm_register_model()}}. Usage is the token
count and the cost ellmer computes, with the qualification that ellmer
prices audio tokens at the text rate. Gemini's dedicated transcription
model, \code{gemini-3.5-transcribe}, cannot be reached through ellmer yet.
}

No dollar cost is computed for a transcription: ellmer has no rates for
transcription models, and per-minute pricing does not fit a per-token
table. The usage is recorded as reported so it can be costed by hand.
}

\section{Names and identifiers}{


The names of the result become the \code{.id} of each unit when it is coded,
and the document names when the vector is made a corpus. A supplied name
is kept exactly. An unnamed local file is named by its basename; an
unnamed URL is named \code{text1}, \code{text2}, ... by its position in \code{x}. The
resolved names must be unique and non-empty: two files that share a
basename need names supplied, and the error says so.
}

\section{Failures}{


Requests run in parallel. Under \code{on_error = "continue"} every file is
attempted and the result has an element for each, \code{NA} where the
transcription failed, with the provider's message in the \code{.error} column
of the provenance table. \code{"return"} stops submitting after the first
failure and marks the files it never sent as such; \code{"stop"} raises the
first error. Validation of the arguments, the files and the model all
happen before anything is downloaded or sent, and abort whatever
\code{on_error} says.

A missing transcript passed to \code{\link[=qlm_code]{qlm_code()}} is never sent to the model:
its unit is recorded as failed with the transcription's reason, and
\code{\link[=qlm_backfill]{qlm_backfill()}} leaves it alone. Transcribe the file again, by position
(\code{which(is.na(transcripts))}), and assign the new text and its provenance
at that position, or concatenate independent runs with \code{c()}.
}

\section{URLs}{


An element of \code{x} that is an \verb{http://} or \verb{https://} URL is downloaded
to a temporary file, which is removed when the function returns. The
hash and size recorded are those of the downloaded bytes, and the URL is
recorded, with any credential it carried redacted, in the \code{source_url}
column. The format is read from the URL's path, so a URL with no file
extension is refused before anything is fetched.
}

\examples{
\dontrun{
files <- list.files("recordings", pattern = "\\\\.wav$", full.names = TRUE)
transcripts <- qlm_transcribe(files)
transcripts
attr(transcripts, "provenance")

# Code the transcripts with any provider; the run records the transcription
coded <- qlm_code(transcripts, codebook_sentiment, model = "anthropic/claude-sonnet-5")
qlm_trail(coded, path = "sentiment_trail")

# A Gemini chat model as the transcriber, with a language hint
transcripts <- qlm_transcribe(files, model = "google_gemini/gemini-2.5-flash",
                              language = "fr")

# A recording on the web, named so the name becomes its .id
url <- c(harvard = "https://www.voiptroubleshooter.com/open_speech/american/OSR_us_000_0010_8k.wav")
qlm_transcribe(url)
}

}
\seealso{
\code{\link[=qlm_code]{qlm_code()}} for coding the transcripts, and its "Audio input"
section for the single-pass route; \code{\link[=qlm_trail]{qlm_trail()}} for the record a coded
transcript leaves.
}
