% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provider_registry.R
\name{qlm_register_provider}
\alias{qlm_register_provider}
\title{Register an OpenAI-compatible endpoint}
\usage{
qlm_register_provider(provider, base_url, api_key_env, overwrite = FALSE)
}
\arguments{
\item{provider}{A lower-case prefix containing letters, digits, underscores
or hyphens, starting with a letter.}

\item{base_url}{An HTTP(S) API base URL, without credentials, a query or a
fragment. Include the API path, for example \code{"https://example.org/v1"}.}

\item{api_key_env}{Name of the environment variable holding the API key.
ellmer reads its value when building the chat and making requests, and
sends it as a bearer token.}

\item{overwrite}{Replace an existing registration? Defaults to \code{FALSE}.
Native ellmer providers cannot be replaced.}
}
\value{
The endpoint definition, invisibly.
}
\description{
Register a provider prefix for use in \code{model = "provider/model"} with
\code{\link[=qlm_code]{qlm_code()}} and \code{\link[=qlm_segment]{qlm_segment()}}. Registration lasts for this R session.
Native providers in the installed ellmer always take precedence.
}
\details{
Built-in prefixes are \code{dashscope} (Alibaba Model Studio, Singapore),
\code{dashscope-cn} (Beijing), \code{moonshot} (Moonshot's international endpoint),
and \code{zai} (Z.AI's general API, not its Coding Plan endpoint).
They read \code{DASHSCOPE_API_KEY},
\code{DASHSCOPE_API_KEY}, \code{MOONSHOT_API_KEY}, and \code{ZHIPU_API_KEY}, respectively.
Keys must belong to the endpoint's region. Model names are passed through
unchanged, including any slashes; the registry does not maintain a model
catalogue, capability list or prices. Supply \code{prices} to \code{\link[=qlm_code]{qlm_code()}}
when needed.

Explicit \code{credentials} (or ellmer's deprecated \code{api_key}) overrides the
registered environment variable. Overriding \code{base_url} to a different URL
requires explicit credentials, so a registered key cannot travel to another
endpoint by accident. Use \code{credentials = function() ""} for an endpoint
requiring no authentication.

Runs record the requested prefix and the effective
\code{openai_compatible/model}, base URL and credential source. Replication and
backfill reuse the recorded endpoint without consulting the registry.
Supplying a replacement \code{model} resolves that model afresh. Registry
entries contain environment variable names, never key values.
}
\examples{
qlm_register_provider("my_gateway", "https://example.org/v1", "GATEWAY_KEY")
\dontrun{
qlm_code(texts, codebook, model = "dashscope/qwen-plus")
qlm_code(texts, codebook, model = "my_gateway/organisation/model")
}
}
