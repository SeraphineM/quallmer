% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_classification.R
\name{confusion_components}
\alias{confusion_components}
\title{Per-class confusion-matrix components}
\usage{
confusion_components(truth, estimate)
}
\description{
Internal helper. Returns a data.frame with one row per class
(the union of levels in \code{truth} and \code{estimate}), giving TP, FP, FN,
and the truth-side count \code{n_truth} for that class.
}
\details{
Layout convention: \code{table(truth, estimate)} -- rows = truth, columns
= estimate. Then for class c:
\itemize{
\item TP_c = diagonal entry
\item FP_c = column sum minus diagonal (predicted as c but truth is not)
\item FN_c = row sum minus diagonal (truth is c but predicted differently)
}
}
\keyword{internal}
